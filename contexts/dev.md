# FF Development Context
> Inject when building new features or services.

Mode: DEVELOPMENT
- Generate code with full standards compliance
- Include unit tests alongside implementation (TDD preferred)
- Flag any SLA risk on inline scoring path immediately
- Use FraudDecision enum, never raw strings
- All DB queries include tenant_id filter
